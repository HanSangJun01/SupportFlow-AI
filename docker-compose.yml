services:
  mongodb:
    image: mongo:7
    container_name: supportflow-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  backend:
    build:
      context: ./backend-spring
    container_name: supportflow-backend
    environment:
      SPRING_DATA_MONGODB_URI: mongodb://mongodb:27017/supportflow
    ports:
      - "8080:8080"
    depends_on:
      - mongodb

  redis:
    image: redis:7
    container_name: supportflow-redis
    ports:
      - "6379:6379"

volumes:
  mongo-data:
