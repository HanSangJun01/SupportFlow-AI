---
phase: 01-backend-foundation
plan: 01-02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend-spring/src/main/java/com/supportflow/common/ApiErrorResponse.java
  - backend-spring/src/main/java/com/supportflow/common/GlobalExceptionHandler.java
  - backend-spring/src/main/java/com/supportflow/tenant/Tenant.java
  - backend-spring/src/main/java/com/supportflow/tenant/TenantStatus.java
  - backend-spring/src/main/java/com/supportflow/tenant/TenantRepository.java
  - backend-spring/src/main/java/com/supportflow/tenant/TenantService.java
  - backend-spring/src/main/java/com/supportflow/tenant/TenantController.java
  - backend-spring/src/main/java/com/supportflow/ticket/Ticket.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketPriority.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketStatus.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketRepository.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketService.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketController.java
  - backend-spring/src/test/java/com/supportflow/tenant/TenantApiIntegrationTest.java
  - backend-spring/src/test/java/com/supportflow/ticket/TicketApiIntegrationTest.java
autonomous: true
requirements: [TEN-01, TICK-01, TICK-02, TICK-03, TICK-05, ISO-01, ISO-02]
user_setup: []
must_haves:
  truths:
    - "Super admin can create, list, and view tenant workspaces through REST APIs."
    - "Support agent can create, list, and view tickets only through tenant-scoped URL paths."
    - "Every persisted ticket stores `tenantId`, and ticket queries include `tenantId` constraints."
    - "Ticket list filters include status, priority, assigneeId, createdFrom, and createdTo."
  artifacts:
    - path: "backend-spring/src/main/java/com/supportflow/tenant/TenantController.java"
      provides: "Tenant create/list/detail REST API"
      contains: "/api/v1/tenants"
    - path: "backend-spring/src/main/java/com/supportflow/ticket/TicketController.java"
      provides: "Tenant-scoped ticket create/list/detail REST API"
      contains: "/api/v1/tenants/{tenantId}/tickets"
    - path: "backend-spring/src/main/java/com/supportflow/ticket/Ticket.java"
      provides: "Ticket Mongo document with tenantId"
      contains: "tenantId"
    - path: "backend-spring/src/test/java/com/supportflow/ticket/TicketApiIntegrationTest.java"
      provides: "HTTP integration tests for ticket API behavior"
      contains: "createdFrom"
  key_links:
    - from: "backend-spring/src/main/java/com/supportflow/ticket/TicketController.java"
      to: "backend-spring/src/main/java/com/supportflow/ticket/TicketService.java"
      via: "Controller passes path `tenantId` into service calls"
      pattern: "tenantId"
    - from: "backend-spring/src/main/java/com/supportflow/ticket/TicketService.java"
      to: "backend-spring/src/main/java/com/supportflow/ticket/TicketRepository.java"
      via: "Service uses tenant-aware repository methods"
      pattern: "findByTenantId"
---

<objective>
Implement tenant workspace basics and core tenant-scoped ticket APIs.

Purpose: Deliver the first real backend vertical slice: create a tenant, create a ticket under that tenant, and read it back through tenant-scoped HTTP paths.
Output: Tenant and ticket domain documents, repositories, services, controllers, DTO validation, and HTTP integration tests.
</objective>

<execution_context>
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-VALIDATION.md
@.planning/phases/01-backend-foundation/01-01-SUMMARY.md
</context>

<threat_model>
| Threat ID | STRIDE | Component | Disposition | Mitigation Plan |
|-----------|--------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Tenant create API | mitigate | Validate `name` and `slug`, generate server-side IDs, and default status to `ACTIVE` per D-02. |
| T-02-02 | Information Disclosure | Ticket detail/list APIs | mitigate | Require `tenantId` in path and call repository methods constrained by `tenantId` per D-01, ISO-01, and ISO-02. |
| T-02-03 | Repudiation | API error responses | mitigate | Return structured error responses with timestamp, status, error, message, and path for invalid requests. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement tenant workspace APIs</name>
  <files>backend-spring/src/main/java/com/supportflow/common/ApiErrorResponse.java, backend-spring/src/main/java/com/supportflow/common/GlobalExceptionHandler.java, backend-spring/src/main/java/com/supportflow/tenant/Tenant.java, backend-spring/src/main/java/com/supportflow/tenant/TenantStatus.java, backend-spring/src/main/java/com/supportflow/tenant/TenantRepository.java, backend-spring/src/main/java/com/supportflow/tenant/TenantService.java, backend-spring/src/main/java/com/supportflow/tenant/TenantController.java, backend-spring/src/test/java/com/supportflow/tenant/TenantApiIntegrationTest.java</files>
  <read_first>backend-spring/pom.xml, backend-spring/src/main/java/com/supportflow/SupportFlowApplication.java, .planning/phases/01-backend-foundation/01-CONTEXT.md</read_first>
  <action>Implement tenant APIs locked by D-02 and D-03. `Tenant` is a Mongo document with `id`, `name`, `slug`, optional `description`, `status`, `createdAt`, and `updatedAt`. `TenantStatus` includes `ACTIVE`. `POST /api/v1/tenants` accepts JSON `{ "name": "...", "slug": "...", "description": "..." }`, requires nonblank `name` and `slug`, generates ID server-side, defaults `status` to `ACTIVE`, and returns HTTP 201. `GET /api/v1/tenants` returns all tenants sorted by creation time or slug. `GET /api/v1/tenants/{tenantId}` returns HTTP 200 for existing tenants and HTTP 404 for missing tenants. Add a global exception handler for validation and not-found errors. Do not implement tenant update endpoints because D-04 keeps tenant update out of Phase 1.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw test -Dtest=TenantApiIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `TenantController.java` contains `@PostMapping` and `@GetMapping("/{tenantId}")`.
    - `TenantStatus.java` contains `ACTIVE`.
    - `TenantApiIntegrationTest.java` asserts POST `/api/v1/tenants` returns status 201.
    - `TenantApiIntegrationTest.java` asserts tenant create response contains `"status":"ACTIVE"`.
    - `TenantApiIntegrationTest.java` asserts missing tenant detail returns 404.
  </acceptance_criteria>
  <done>Tenant create, list, and detail APIs pass HTTP integration tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tenant-scoped ticket create/list/detail APIs</name>
  <files>backend-spring/src/main/java/com/supportflow/ticket/Ticket.java, backend-spring/src/main/java/com/supportflow/ticket/TicketPriority.java, backend-spring/src/main/java/com/supportflow/ticket/TicketStatus.java, backend-spring/src/main/java/com/supportflow/ticket/TicketRepository.java, backend-spring/src/main/java/com/supportflow/ticket/TicketService.java, backend-spring/src/main/java/com/supportflow/ticket/TicketController.java, backend-spring/src/test/java/com/supportflow/ticket/TicketApiIntegrationTest.java</files>
  <read_first>backend-spring/src/main/java/com/supportflow/tenant/Tenant.java, backend-spring/src/main/java/com/supportflow/tenant/TenantService.java, .planning/phases/01-backend-foundation/01-CONTEXT.md</read_first>
  <action>Implement ticket APIs per D-01, D-08, D-09, D-10, and D-12. `Ticket` is a Mongo document with `id`, `tenantId`, `subject`, `customerName`, `customerEmail`, `customerMessage`, `status`, nullable `category`, nullable `priority`, nullable `assigneeId`, `createdAt`, and `updatedAt`. `TicketStatus` includes `NEW`, `TRIAGED`, `IN_PROGRESS`, `ANSWERED`, `CLOSED`. `TicketPriority` includes `LOW`, `NORMAL`, `HIGH`, `URGENT`. `POST /api/v1/tenants/{tenantId}/tickets` requires `subject`, `customerName`, `customerEmail`, and `customerMessage`, verifies the tenant exists, sets status `NEW`, stores `tenantId`, and returns HTTP 201. `GET /api/v1/tenants/{tenantId}/tickets` supports query params `status`, `priority`, `assigneeId`, `createdFrom`, and `createdTo`; all repository methods must include `tenantId`. `GET /api/v1/tenants/{tenantId}/tickets/{ticketId}` returns only a ticket matching both IDs, otherwise 404. Keep full operational enrichment, urgency, sentiment, SLA-risk fields, and workflow history out of Phase 1 per D-11.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw test -Dtest=TicketApiIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `TicketController.java` contains `/api/v1/tenants/{tenantId}/tickets`.
    - `Ticket.java` contains `private String tenantId`.
    - `TicketStatus.java` contains `NEW`, `TRIAGED`, `IN_PROGRESS`, `ANSWERED`, and `CLOSED`.
    - `TicketRepository.java` contains `findByTenantId`.
    - `TicketApiIntegrationTest.java` asserts ticket create response contains `"status":"NEW"`.
    - `TicketApiIntegrationTest.java` asserts list query supports `status`, `priority`, `assigneeId`, `createdFrom`, and `createdTo`.
  </acceptance_criteria>
  <done>Ticket create, list, and detail APIs pass HTTP integration tests and are tenant-scoped by URL path.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend-spring && ./mvnw test -Dtest=TenantApiIntegrationTest` exits 0.
- [ ] `cd backend-spring && ./mvnw test -Dtest=TicketApiIntegrationTest` exits 0.
- [ ] Ticket repository methods include `tenantId` constraints.
- [ ] Tenant update APIs are not added in Phase 1.
</verification>

<success_criteria>
- Tenant APIs cover create, list, and detail only.
- Ticket APIs cover create, list, and detail through tenant-scoped paths.
- Ticket list filters satisfy TICK-02 and D-12.
- Persisted tickets include `tenantId`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-02-SUMMARY.md`.
</output>
