---
phase: 01-backend-foundation
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend-spring/pom.xml
  - backend-spring/mvnw
  - backend-spring/mvnw.cmd
  - backend-spring/.mvn/wrapper/maven-wrapper.properties
  - backend-spring/src/main/java/com/supportflow/SupportFlowApplication.java
  - backend-spring/src/main/resources/application.yml
  - backend-spring/src/test/java/com/supportflow/SupportFlowApplicationTests.java
  - docker-compose.yml
autonomous: true
requirements: [QUAL-06]
user_setup: []
must_haves:
  truths:
    - "The Spring Boot backend can be built and tested from backend-spring with Maven."
    - "The backend has MongoDB configuration but does not require Redis for Phase 1 startup."
    - "Docker Compose provides a reproducible local MongoDB-backed development path."
  artifacts:
    - path: "backend-spring/pom.xml"
      provides: "Spring Boot 3.5.14 Maven build with web, validation, data-mongodb, actuator, springdoc, and tests"
      contains: "3.5.14"
    - path: "backend-spring/src/main/java/com/supportflow/SupportFlowApplication.java"
      provides: "Spring Boot application entrypoint"
      contains: "@SpringBootApplication"
    - path: "backend-spring/src/main/resources/application.yml"
      provides: "MongoDB and server configuration"
      contains: "spring.data.mongodb"
    - path: "docker-compose.yml"
      provides: "Local MongoDB service and backend run wiring"
      contains: "mongodb"
  key_links:
    - from: "backend-spring/src/main/resources/application.yml"
      to: "docker-compose.yml"
      via: "MongoDB URI points at the compose MongoDB service in local profile"
      pattern: "mongodb://.*mongodb"
---

<objective>
Set up the Spring Boot backend foundation and local MongoDB runtime path.

Purpose: Establish the executable backend skeleton every later Phase 1 plan depends on.
Output: Maven Spring Boot project, application entrypoint, MongoDB configuration, base smoke test, and Docker Compose local run support.
</objective>

<execution_context>
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-VALIDATION.md
@.planning/phases/01-backend-foundation/01-SKELETON.md
@docker-compose.yml
</context>

<threat_model>
| Threat ID | STRIDE | Component | Disposition | Mitigation Plan |
|-----------|--------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `application.yml` MongoDB configuration | mitigate | Use explicit `spring.data.mongodb.uri` values for local/test profiles and avoid hard-coded credentials in committed config. |
| T-01-02 | Denial of Service | Local Docker Compose backend/MongoDB path | accept | Phase 1 is local-only; no production exposure or rate limiting is claimed. |
| T-01-03 | Information Disclosure | Actuator and generated docs dependencies | mitigate | Do not expose sensitive actuator endpoints; keep Phase 1 actuator limited to health/info defaults unless later plans explicitly open more. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Spring Boot Maven scaffold</name>
  <files>backend-spring/pom.xml, backend-spring/mvnw, backend-spring/mvnw.cmd, backend-spring/.mvn/wrapper/maven-wrapper.properties, backend-spring/src/main/java/com/supportflow/SupportFlowApplication.java, backend-spring/src/test/java/com/supportflow/SupportFlowApplicationTests.java</files>
  <read_first>.planning/phases/01-backend-foundation/01-RESEARCH.md, .planning/phases/01-backend-foundation/01-SKELETON.md</read_first>
  <action>Create a Maven Spring Boot project under `backend-spring` using groupId `com.supportflow`, artifactId `backend-spring`, Java `21`, Spring Boot parent version `3.5.14`, and dependencies `spring-boot-starter-web`, `spring-boot-starter-data-mongodb`, `spring-boot-starter-validation`, `spring-boot-starter-actuator`, `org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.17`, `spring-boot-starter-test`, `spring-boot-testcontainers`, `org.testcontainers:junit-jupiter`, and `org.testcontainers:mongodb`. Create `SupportFlowApplication` in package `com.supportflow` with `@SpringBootApplication`. Add `SupportFlowApplicationTests` with a `contextLoads()` test. Generate or add Maven wrapper files so `./mvnw test` works from `backend-spring`.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw test</automated>
  </verify>
  <acceptance_criteria>
    - `backend-spring/pom.xml` contains `<version>3.5.14</version>` under the Spring Boot parent.
    - `backend-spring/pom.xml` contains `spring-boot-starter-data-mongodb`.
    - `backend-spring/pom.xml` contains `springdoc-openapi-starter-webmvc-ui`.
    - `backend-spring/src/main/java/com/supportflow/SupportFlowApplication.java` contains `package com.supportflow;`.
    - `cd backend-spring && ./mvnw test` exits 0.
  </acceptance_criteria>
  <done>The backend scaffold builds through the Maven wrapper and has a passing Spring context smoke test.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MongoDB configuration and local compose path</name>
  <files>backend-spring/src/main/resources/application.yml, docker-compose.yml</files>
  <read_first>docker-compose.yml, .planning/phases/01-backend-foundation/01-CONTEXT.md, .planning/phases/01-backend-foundation/01-RESEARCH.md</read_first>
  <action>Create `application.yml` with `spring.application.name: supportflow-backend`, `server.port: 8080`, `spring.data.mongodb.uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/supportflow}`, and springdoc enabled at `/v3/api-docs` and `/swagger-ui.html`. Update `docker-compose.yml` so local development can run MongoDB and the backend together: keep service name `mongodb`, expose `27017:27017`, add a `backend` service built from `backend-spring` or runnable through the Maven wrapper, set `SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/supportflow`, expose `8080:8080`, and add `depends_on: [mongodb]`. Do not add any Redis dependency to the backend; Redis may remain in compose only as a future unused service if already present.</action>
  <verify>
    <automated>docker compose config && cd backend-spring && ./mvnw test</automated>
  </verify>
  <acceptance_criteria>
    - `backend-spring/src/main/resources/application.yml` contains `mongodb://localhost:27017/supportflow`.
    - `docker-compose.yml` contains `SPRING_DATA_MONGODB_URI=mongodb://mongodb:27017/supportflow`.
    - `docker-compose.yml` contains `8080:8080`.
    - `backend-spring/pom.xml` does not contain `spring-boot-starter-data-redis`.
    - `docker compose config` exits 0.
  </acceptance_criteria>
  <done>Local configuration supports backend plus MongoDB execution without introducing a Phase 1 Redis runtime requirement.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend-spring && ./mvnw test` exits 0.
- [ ] `docker compose config` exits 0.
- [ ] `backend-spring/pom.xml` contains Spring Boot `3.5.14` and springdoc `2.8.17`.
- [ ] Backend config includes MongoDB URI and no Redis dependency.
</verification>

<success_criteria>
- Spring Boot Maven scaffold exists and builds.
- MongoDB local configuration is committed.
- Docker Compose can be parsed and includes backend plus MongoDB local run wiring.
- No Phase 1 backend dependency on Redis is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-01-SUMMARY.md`.
</output>

