---
phase: 01-backend-foundation
plan: 01-04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - backend-spring/src/main/java/com/supportflow/common/OpenApiConfig.java
  - backend-spring/src/main/java/com/supportflow/tenant/TenantController.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketController.java
  - backend-spring/src/test/java/com/supportflow/common/OpenApiDocumentationTest.java
  - backend-spring/src/test/java/com/supportflow/FoundationVerificationTest.java
  - docs/sdd/phase-01-backend-foundation-api.md
  - README.md
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-06, TEN-01, TICK-01, TICK-02, TICK-03, TICK-05, FLOW-01, FLOW-02, ISO-01, ISO-02, ISO-04]
user_setup: []
must_haves:
  truths:
    - "OpenAPI docs expose Phase 1 tenant and ticket routes through `/v3/api-docs` and Swagger UI."
    - "The full Maven verification suite covers domain lifecycle rules and MongoDB-backed HTTP integration tests."
    - "Repository and HTTP tests prove tenant isolation for implemented foundation APIs."
    - "README or docs describe the local run path for MongoDB and backend APIs."
  artifacts:
    - path: "backend-spring/src/main/java/com/supportflow/common/OpenApiConfig.java"
      provides: "OpenAPI metadata configuration"
      contains: "SupportFlow AI Backend API"
    - path: "backend-spring/src/test/java/com/supportflow/common/OpenApiDocumentationTest.java"
      provides: "Automated API docs verification"
      contains: "/v3/api-docs"
    - path: "docs/sdd/phase-01-backend-foundation-api.md"
      provides: "Phase 1 backend API contract notes"
      contains: "PATCH /api/v1/tenants/{tenantId}/tickets/{ticketId}/status"
    - path: "README.md"
      provides: "Local development run instructions"
      contains: "docker compose"
  key_links:
    - from: "backend-spring/src/main/java/com/supportflow/common/OpenApiConfig.java"
      to: "backend-spring/src/main/java/com/supportflow/tenant/TenantController.java"
      via: "OpenAPI scans controller annotations"
      pattern: "OpenAPI"
    - from: "docs/sdd/phase-01-backend-foundation-api.md"
      to: "backend-spring/src/test/java/com/supportflow/common/OpenApiDocumentationTest.java"
      via: "Documentation paths are asserted in `/v3/api-docs`"
      pattern: "/api/v1/tenants"
---

<objective>
Add REST API documentation and foundation unit/integration verification.

Purpose: Turn the implemented backend foundation into a documented, reproducible, test-proven contract for later frontend and AI phases.
Output: OpenAPI configuration, API docs assertions, foundation verification coverage, README/local run docs, and Phase 1 API contract notes.
</objective>

<execution_context>
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-VALIDATION.md
@.planning/phases/01-backend-foundation/01-03-SUMMARY.md
</context>

<threat_model>
| Threat ID | STRIDE | Component | Disposition | Mitigation Plan |
|-----------|--------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | OpenAPI docs | mitigate | Document only Phase 1 routes and schemas; do not add auth claims, secrets, or internal MongoDB details to docs. |
| T-04-02 | Repudiation | Verification evidence | mitigate | Add automated docs and full-suite tests so Phase 1 behavior is reproducible from commands, not manual inspection. |
| T-04-03 | Tampering | README/local run commands | mitigate | Keep commands explicit and local-only; do not instruct users to disable tests or bypass tenant isolation checks. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Configure and verify OpenAPI documentation</name>
  <files>backend-spring/src/main/java/com/supportflow/common/OpenApiConfig.java, backend-spring/src/main/java/com/supportflow/tenant/TenantController.java, backend-spring/src/main/java/com/supportflow/ticket/TicketController.java, backend-spring/src/test/java/com/supportflow/common/OpenApiDocumentationTest.java</files>
  <read_first>backend-spring/pom.xml, backend-spring/src/main/java/com/supportflow/tenant/TenantController.java, backend-spring/src/main/java/com/supportflow/ticket/TicketController.java, .planning/phases/01-backend-foundation/01-CONTEXT.md</read_first>
  <action>Add Springdoc OpenAPI documentation per D-13. Create `OpenApiConfig` with title `SupportFlow AI Backend API`, version `v1`, and description `Backend foundation APIs for tenant workspaces and tenant-scoped tickets`. Add concise OpenAPI annotations to tenant and ticket controllers for the seven Phase 1 routes. Add `OpenApiDocumentationTest` that starts the Spring context, calls `GET /v3/api-docs`, and asserts the JSON contains `/api/v1/tenants`, `/api/v1/tenants/{tenantId}`, `/api/v1/tenants/{tenantId}/tickets`, `/api/v1/tenants/{tenantId}/tickets/{ticketId}`, and `/api/v1/tenants/{tenantId}/tickets/{ticketId}/status`.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw test -Dtest=OpenApiDocumentationTest</automated>
  </verify>
  <acceptance_criteria>
    - `OpenApiConfig.java` contains `SupportFlow AI Backend API`.
    - `OpenApiDocumentationTest.java` contains `/v3/api-docs`.
    - `OpenApiDocumentationTest.java` asserts `/api/v1/tenants/{tenantId}/tickets/{ticketId}/status`.
    - `TenantController.java` or `TicketController.java` contains at least one `@Operation` annotation.
  </acceptance_criteria>
  <done>OpenAPI docs are generated and route coverage is verified by automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add foundation docs and full verification harness</name>
  <files>backend-spring/src/test/java/com/supportflow/FoundationVerificationTest.java, docs/sdd/phase-01-backend-foundation-api.md, README.md</files>
  <read_first>README.md, docker-compose.yml, .planning/phases/01-backend-foundation/01-VALIDATION.md, backend-spring/src/test/java/com/supportflow/ticket/TenantIsolationIntegrationTest.java, backend-spring/src/test/java/com/supportflow/ticket/TicketStatusTransitionPolicyTest.java</read_first>
  <action>Add `FoundationVerificationTest` or equivalent suite-level tests/checks that reference the Phase 1 invariants: tenant create/list/detail exists, ticket create/list/detail exists, status transitions are enforced, tenant isolation tests exist, and OpenAPI docs are available. Ensure tenant isolation proof uses full HTTP API integration tests against Spring Boot with a MongoDB Testcontainer per D-14, and keep pure lifecycle rules covered by unit tests while cross-tenant read/write denial is verified through HTTP endpoint tests per D-15. Create `docs/sdd/phase-01-backend-foundation-api.md` listing request/response expectations for all seven Phase 1 endpoints, required fields, filters, allowed lifecycle transitions, and deferred Phase 2 items. Update `README.md` with local commands: `docker compose up -d mongodb`, `cd backend-spring && ./mvnw spring-boot:run`, `cd backend-spring && ./mvnw test`, `cd backend-spring && ./mvnw verify`, and docs URLs `http://localhost:8080/v3/api-docs` and `http://localhost:8080/swagger-ui.html`.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw verify</automated>
  </verify>
  <acceptance_criteria>
    - `docs/sdd/phase-01-backend-foundation-api.md` contains `POST /api/v1/tenants`.
    - `docs/sdd/phase-01-backend-foundation-api.md` contains `GET /api/v1/tenants/{tenantId}/tickets`.
    - `docs/sdd/phase-01-backend-foundation-api.md` contains `ANSWERED -> IN_PROGRESS`.
    - `README.md` contains `docker compose up -d mongodb`.
    - `README.md` contains `./mvnw verify`.
    - `cd backend-spring && ./mvnw verify` exits 0.
  </acceptance_criteria>
  <done>Phase 1 behavior is documented and the full automated verification suite passes.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend-spring && ./mvnw test -Dtest=OpenApiDocumentationTest` exits 0.
- [ ] `cd backend-spring && ./mvnw verify` exits 0.
- [ ] README contains local run, test, and docs URLs.
- [ ] API contract doc lists all Phase 1 endpoints and deferred Phase 2 items.
</verification>

<success_criteria>
- REST-first API docs are generated and verified.
- Unit and integration tests cover lifecycle, tenant APIs, ticket APIs, and tenant isolation.
- Local development instructions are reproducible.
- All Phase 1 requirements appear in at least one plan's `requirements` field.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-04-SUMMARY.md`.
</output>
