---
phase: 01-backend-foundation
plan: 01-03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - backend-spring/src/main/java/com/supportflow/ticket/TicketStatusTransitionPolicy.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketService.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketController.java
  - backend-spring/src/main/java/com/supportflow/ticket/TicketRepository.java
  - backend-spring/src/test/java/com/supportflow/ticket/TicketStatusTransitionPolicyTest.java
  - backend-spring/src/test/java/com/supportflow/ticket/TenantIsolationIntegrationTest.java
autonomous: true
requirements: [FLOW-01, FLOW-02, ISO-01, ISO-02, ISO-04]
user_setup: []
must_haves:
  truths:
    - "Ticket lifecycle states are exactly NEW, TRIAGED, IN_PROGRESS, ANSWERED, and CLOSED."
    - "Allowed transitions are NEW->TRIAGED, TRIAGED->IN_PROGRESS, IN_PROGRESS->ANSWERED, ANSWERED->CLOSED, and ANSWERED->IN_PROGRESS."
    - "Invalid lifecycle transitions are rejected by backend rules."
    - "Cross-tenant ticket detail reads and status mutations are denied through HTTP integration tests."
  artifacts:
    - path: "backend-spring/src/main/java/com/supportflow/ticket/TicketStatusTransitionPolicy.java"
      provides: "Explicit lifecycle transition rules"
      contains: "ANSWERED"
    - path: "backend-spring/src/test/java/com/supportflow/ticket/TicketStatusTransitionPolicyTest.java"
      provides: "Unit coverage for allowed and denied transitions"
      contains: "CLOSED"
    - path: "backend-spring/src/test/java/com/supportflow/ticket/TenantIsolationIntegrationTest.java"
      provides: "HTTP-level tenant isolation proof"
      contains: "crossTenant"
  key_links:
    - from: "backend-spring/src/main/java/com/supportflow/ticket/TicketController.java"
      to: "backend-spring/src/main/java/com/supportflow/ticket/TicketStatusTransitionPolicy.java"
      via: "Status update endpoint calls service, service calls transition policy"
      pattern: "transition"
    - from: "backend-spring/src/main/java/com/supportflow/ticket/TicketRepository.java"
      to: "backend-spring/src/test/java/com/supportflow/ticket/TenantIsolationIntegrationTest.java"
      via: "Integration test proves tenant-aware repository use through HTTP"
      pattern: "findByTenantIdAndId"
---

<objective>
Implement ticket lifecycle transition rules and tenant-aware persistence boundaries.

Purpose: Make ticket statuses meaningful and prove tenant isolation at the HTTP boundary before later workflow and AI phases build on tickets.
Output: Explicit transition policy, status update endpoint, tenant-aware repository methods, unit tests, and cross-tenant HTTP integration tests.
</objective>

<execution_context>
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md
@.planning/phases/01-backend-foundation/01-VALIDATION.md
@.planning/phases/01-backend-foundation/01-02-SUMMARY.md
</context>

<threat_model>
| Threat ID | STRIDE | Component | Disposition | Mitigation Plan |
|-----------|--------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Ticket status update endpoint | mitigate | Validate all requested transitions through `TicketStatusTransitionPolicy`; reject invalid transitions with HTTP 400. |
| T-03-02 | Information Disclosure | Cross-tenant ticket access | mitigate | Use `findByTenantIdAndId` for reads and mutations; return 404 when `ticketId` exists under a different tenant. |
| T-03-03 | Elevation of Privilege | Tenant path parameter before authentication | mitigate | Treat path `tenantId` as the sole Phase 1 tenant scope and prove isolation with negative HTTP tests; do not claim user-level authorization. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle transition policy and status endpoint</name>
  <files>backend-spring/src/main/java/com/supportflow/ticket/TicketStatusTransitionPolicy.java, backend-spring/src/main/java/com/supportflow/ticket/TicketService.java, backend-spring/src/main/java/com/supportflow/ticket/TicketController.java, backend-spring/src/test/java/com/supportflow/ticket/TicketStatusTransitionPolicyTest.java</files>
  <read_first>backend-spring/src/main/java/com/supportflow/ticket/TicketStatus.java, backend-spring/src/main/java/com/supportflow/ticket/TicketService.java, backend-spring/src/main/java/com/supportflow/ticket/TicketController.java, .planning/phases/01-backend-foundation/01-CONTEXT.md</read_first>
  <action>Create `TicketStatusTransitionPolicy` with `boolean canTransition(TicketStatus from, TicketStatus to)` and `void validateTransition(TicketStatus from, TicketStatus to)`. Allow exactly `NEW -> TRIAGED`, `TRIAGED -> IN_PROGRESS`, `IN_PROGRESS -> ANSWERED`, `ANSWERED -> CLOSED`, and `ANSWERED -> IN_PROGRESS` per D-06. Reject all transitions from `CLOSED` per D-07. Add `PATCH /api/v1/tenants/{tenantId}/tickets/{ticketId}/status` accepting `{ "status": "TRIAGED" }`, loading the ticket with both tenant ID and ticket ID, validating the transition, updating `status` and `updatedAt`, and returning the updated ticket. Return HTTP 400 for invalid transitions and 404 for missing tenant/ticket.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw test -Dtest=TicketStatusTransitionPolicyTest</automated>
  </verify>
  <acceptance_criteria>
    - `TicketStatusTransitionPolicy.java` contains `NEW`, `TRIAGED`, `IN_PROGRESS`, `ANSWERED`, and `CLOSED`.
    - `TicketStatusTransitionPolicy.java` contains logic for `ANSWERED -> IN_PROGRESS`.
    - `TicketStatusTransitionPolicyTest.java` asserts `CLOSED -> IN_PROGRESS` is rejected.
    - `TicketController.java` contains `@PatchMapping("/{ticketId}/status")`.
    - Invalid transition tests receive HTTP 400 in an integration test or controller test.
  </acceptance_criteria>
  <done>Status transitions are enforced by a domain policy and exposed through a tenant-scoped status update endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Prove tenant-aware persistence and cross-tenant denial</name>
  <files>backend-spring/src/main/java/com/supportflow/ticket/TicketRepository.java, backend-spring/src/main/java/com/supportflow/ticket/TicketService.java, backend-spring/src/test/java/com/supportflow/ticket/TenantIsolationIntegrationTest.java</files>
  <read_first>backend-spring/src/main/java/com/supportflow/ticket/TicketRepository.java, backend-spring/src/main/java/com/supportflow/ticket/TicketService.java, backend-spring/src/test/java/com/supportflow/ticket/TicketApiIntegrationTest.java, .planning/phases/01-backend-foundation/01-VALIDATION.md</read_first>
  <action>Ensure ticket reads and mutations use repository methods constrained by `tenantId`, including `findByTenantIdAndId`, `findAllByTenantId`, and tenant-scoped filter methods. Add `TenantIsolationIntegrationTest` using Spring Boot HTTP tests and MongoDB Testcontainer. Test setup must create tenant A and tenant B, create a ticket under tenant A, assert tenant A can read it, assert `GET /api/v1/tenants/{tenantB}/tickets/{ticketA}` returns 404, assert `PATCH /api/v1/tenants/{tenantB}/tickets/{ticketA}/status` returns 404, and assert tenant B list does not include tenant A ticket.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw test -Dtest=TenantIsolationIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `TicketRepository.java` contains `findByTenantIdAndId`.
    - `TicketService.java` does not call a ticket repository method named `findById(` for tenant-scoped reads.
    - `TenantIsolationIntegrationTest.java` contains `tenantA` and `tenantB`.
    - `TenantIsolationIntegrationTest.java` asserts cross-tenant detail returns 404.
    - `TenantIsolationIntegrationTest.java` asserts cross-tenant status update returns 404.
  </acceptance_criteria>
  <done>Cross-tenant read and write denial is proven through HTTP-level integration tests.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend-spring && ./mvnw test -Dtest=TicketStatusTransitionPolicyTest` exits 0.
- [ ] `cd backend-spring && ./mvnw test -Dtest=TenantIsolationIntegrationTest` exits 0.
- [ ] No ticket service method uses tenant-agnostic `findById` for tenant-scoped reads or mutations.
- [ ] Closed-ticket reopen behavior remains rejected.
</verification>

<success_criteria>
- Lifecycle rules exactly match D-05 through D-07.
- Invalid lifecycle transitions are rejected by automated tests.
- Tenant isolation is verified for reads and status mutations.
- Phase 1 does not implement full workflow history.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-03-SUMMARY.md`.
</output>

