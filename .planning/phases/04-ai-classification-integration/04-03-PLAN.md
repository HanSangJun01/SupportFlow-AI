---
phase: 04-ai-classification-integration
plan: 04-03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - docker-compose.yml
  - backend-spring/src/test/java/com/supportflow/ticket/TicketClassificationMongoIntegrationTest.java
  - backend-spring/src/test/java/com/supportflow/common/OpenApiDocumentationTest.java
  - backend-spring/src/test/java/com/supportflow/FoundationVerificationTest.java
  - docs/sdd/phase-04-ai-classification-integration-api.md
  - README.md
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04, QUAL-04]
requirements_addressed: [AI-01, AI-02, AI-03, AI-04, QUAL-04]
user_setup: []
---

<objective>
Add Docker-backed cross-service verification, API documentation, and full Phase 4 verification gates.

User story: As a platform maintainer, I want the Spring backend and FastAPI AI service verified together in a reproducible local environment, so that ticket classification artifacts and auto-applied workflow updates are proven before evidence retrieval and draft generation are added.

Purpose: Harden Phase 4 with Docker Compose service wiring, Testcontainers coverage using the real FastAPI service, OpenAPI/foundation assertions, an SDD API contract, README updates, and full Python plus Maven verification.
Output: compose wiring, Docker-backed backend integration tests, API documentation, foundation checks, and a green full-suite command.
</objective>

<execution_context>
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ai-classification-integration/04-CONTEXT.md
@.planning/phases/04-ai-classification-integration/04-AI-SPEC.md
@.planning/phases/04-ai-classification-integration/04-RESEARCH.md
@.planning/phases/04-ai-classification-integration/04-VALIDATION.md
@.planning/phases/04-ai-classification-integration/04-PATTERNS.md
@.planning/phases/04-ai-classification-integration/04-01-SUMMARY.md
@.planning/phases/04-ai-classification-integration/04-02-SUMMARY.md
@docs/sdd/phase-03-knowledge-base-core-api.md
</context>

<must_haves>
- AI-01/AI-02: Docker-backed tests must prove the backend can submit a tenant-scoped ticket to the real FastAPI classification endpoint and receive category/urgency/sentiment/priority/confidence/version.
- AI-03/AI-04: Tests and docs must prove stored attempts are visible in ticket create/detail/re-analysis responses.
- D-16/D-20/D-22: Tests must prove success auto-applies category/priority and links history to attempt ID; failure attempts do not mutate fields/history.
- QUAL-04: Python classification tests must run as part of phase verification.
- Docker Compose must add `ai-service` without making Redis required for classification.
- SDD and README must explicitly exclude LLM providers, RAG, embeddings, draft generation, async jobs, frontend, auth, and full RBAC for Phase 4.
</must_haves>

<threat_model>
| Threat ID | STRIDE | Component | Disposition | Mitigation Plan |
|-----------|--------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | Cross-service tenant data | mitigate | Docker-backed tests create multiple tenants and assert classification artifacts are only visible through same-tenant ticket routes. |
| T-04-03-02 | Tampering | AI service failure path | mitigate | Integration tests force unavailable/invalid AI behavior and assert category/priority/history do not change. |
| T-04-03-03 | Repudiation | API contract documentation | mitigate | SDD documents attempt fields, trigger, actor, classifier version, error fields, and history link semantics. |
| T-04-03-04 | Denial of Service | Local compose dependency graph | mitigate | Backend uses a short timeout and starts with `ai-service` dependency in compose; tests remain deterministic. |
| T-04-03-05 | Scope Creep | Phase 5/6 AI behavior | mitigate | Docs and tests explicitly exclude RAG, embeddings, draft generation, real LLM providers, async queues, frontend, auth, and full RBAC. |
</threat_model>

<tasks>

<task id="04-03-01" type="auto">
  <name>Task 1: Add Docker Compose AI service wiring and Docker-backed backend integration tests</name>
  <files>docker-compose.yml, backend-spring/src/test/java/com/supportflow/ticket/TicketClassificationMongoIntegrationTest.java</files>
  <read_first>.planning/phases/04-ai-classification-integration/04-CONTEXT.md, .planning/phases/04-ai-classification-integration/04-AI-SPEC.md, .planning/phases/04-ai-classification-integration/04-VALIDATION.md, docker-compose.yml, ai-service-python/Dockerfile, ai-service-python/app/main.py, backend-spring/src/test/java/com/supportflow/ticket/TenantWorkflowMongoIntegrationTest.java, backend-spring/src/test/java/com/supportflow/knowledge/KnowledgeDocumentMongoIntegrationTest.java, backend-spring/src/main/java/com/supportflow/ticket/TicketController.java, backend-spring/src/main/java/com/supportflow/ticket/TicketService.java</read_first>
  <action>Update `docker-compose.yml` with an `ai-service` service built from `./ai-service-python`, container name `supportflow-ai-service`, port mapping `8000:8000`, and no provider API key environment variables. Add backend environment variable `SUPPORTFLOW_AI_CLASSIFICATION_BASE_URL=http://ai-service:8000` or the exact Spring relaxed-binding equivalent for `supportflow.ai.classification.base-url`. Add `ai-service` to backend `depends_on` while keeping `mongodb` and not requiring `redis` for classification. Create `TicketClassificationMongoIntegrationTest` using `@SpringBootTest(webEnvironment = RANDOM_PORT)` and `@Testcontainers(disabledWithoutDocker = true)`. Use `MongoDBContainer("mongo:7")` and a Testcontainers FastAPI container built from `ai-service-python/Dockerfile` with exposed port `8000`. Register Spring properties for Mongo URI and `supportflow.ai.classification.base-url` using the FastAPI container host/port. Seed tenants and operational users through HTTP helpers. Test ticket creation with message `Urgent billing payment failed and I am frustrated` and assert HTTP 201, category `billing`, priority `HIGH` or `URGENT`, one successful classification attempt with `AUTO_ON_CREATE`, urgency/sentiment/confidence/classifierVersion present, ticket category/priority equal the attempt result, and one `AI_CLASSIFICATION_APPLIED` history entry whose `classificationAttemptId` equals the attempt id. Test manual re-analysis on an existing non-closed ticket and assert a second attempt is appended. Test cross-tenant detail cannot read another tenant ticket's attempts. Test unavailable AI service behavior by overriding the client or property in a focused Spring test if spinning down the container is impractical; expected behavior is failed attempt stored and category/priority/history unchanged.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw test -Dtest=TicketClassificationMongoIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains `ai-service`, `./ai-service-python`, `8000:8000`, and `SUPPORTFLOW_AI_CLASSIFICATION_BASE_URL`.
    - `docker-compose.yml` does not contain `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `LANGCHAIN`, `SUPPORTFLOW_AI_REDIS`, or `AI_REDIS`.
    - `backend-spring/src/test/java/com/supportflow/ticket/TicketClassificationMongoIntegrationTest.java` contains `MongoDBContainer("mongo:7")`.
    - `backend-spring/src/test/java/com/supportflow/ticket/TicketClassificationMongoIntegrationTest.java` contains `ImageFromDockerfile` or equivalent Testcontainers Dockerfile build for `ai-service-python`.
    - `backend-spring/src/test/java/com/supportflow/ticket/TicketClassificationMongoIntegrationTest.java` contains assertions for `AUTO_ON_CREATE`, `AI_CLASSIFICATION_APPLIED`, `classificationAttemptId`, `classifierVersion`, `confidence`, and `MANUAL_REANALYSIS`.
    - `backend-spring/src/test/java/com/supportflow/ticket/TicketClassificationMongoIntegrationTest.java` contains cross-tenant assertion coverage.
    - `cd backend-spring && ./mvnw test -Dtest=TicketClassificationMongoIntegrationTest` exits 0 when Docker is running.
  </acceptance_criteria>
  <done>Spring and FastAPI are wired through Docker and verified together over a real local service boundary.</done>
</task>

<task id="04-03-02" type="auto">
  <name>Task 2: Add OpenAPI assertions, SDD API contract, and README run instructions</name>
  <files>backend-spring/src/test/java/com/supportflow/common/OpenApiDocumentationTest.java, backend-spring/src/test/java/com/supportflow/FoundationVerificationTest.java, docs/sdd/phase-04-ai-classification-integration-api.md, README.md</files>
  <read_first>.planning/phases/04-ai-classification-integration/04-CONTEXT.md, .planning/phases/04-ai-classification-integration/04-AI-SPEC.md, .planning/phases/04-ai-classification-integration/04-PATTERNS.md, docs/sdd/phase-03-knowledge-base-core-api.md, README.md, backend-spring/src/test/java/com/supportflow/common/OpenApiDocumentationTest.java, backend-spring/src/test/java/com/supportflow/FoundationVerificationTest.java, backend-spring/src/main/java/com/supportflow/ticket/TicketController.java</read_first>
  <action>Update `OpenApiDocumentationTest` to assert `/v3/api-docs` contains `/api/v1/tenants/{tenantId}/tickets/{ticketId}/classification-attempts` or the exact manual re-analysis route chosen in Plan 04-02. Update `FoundationVerificationTest` to assert the FastAPI service directory exists, `04-AI-SPEC.md` exists, `TicketClassificationAttempt` exists, `AI_CLASSIFICATION_APPLIED` exists, and `TicketClassificationMongoIntegrationTest` exists. Create `docs/sdd/phase-04-ai-classification-integration-api.md` following the Phase 3 SDD style. The SDD must document: FastAPI `GET /health`; FastAPI `POST /api/v1/classifications/tickets`; Spring ticket create auto-classification behavior; Spring ticket detail classification artifact embedding; manual re-analysis endpoint; request fields; response fields; classification attempt fields; success and failure statuses; triggers `AUTO_ON_CREATE` and `MANUAL_REANALYSIS`; `actorUserId` requirement for manual re-analysis; no human actor for automatic classification; classifier version; confidence; AI-applied history event; `classificationAttemptId` history link; closed ticket rejection; inactive tenant behavior; cross-tenant behavior; timeout/unavailable behavior; failed-attempt non-mutation; and exclusions. Update `README.md` with local run/test instructions for `ai-service-python`, Docker Compose including `ai-service`, and full Phase 4 verification commands. The docs must explicitly say Phase 4 has no provider-backed LLM calls, no prompts, no RAG, no embeddings, no vector search, no draft generation, no async jobs, no frontend UI, no authentication, and no full RBAC.</action>
  <verify>
    <automated>cd backend-spring && ./mvnw test -Dtest=OpenApiDocumentationTest,FoundationVerificationTest</automated>
  </verify>
  <acceptance_criteria>
    - `backend-spring/src/test/java/com/supportflow/common/OpenApiDocumentationTest.java` contains `classification-attempts`.
    - `backend-spring/src/test/java/com/supportflow/FoundationVerificationTest.java` contains `TicketClassificationAttempt`, `AI_CLASSIFICATION_APPLIED`, `TicketClassificationMongoIntegrationTest`, and `04-AI-SPEC.md`.
    - `docs/sdd/phase-04-ai-classification-integration-api.md` exists.
    - `docs/sdd/phase-04-ai-classification-integration-api.md` contains `/health`, `/api/v1/classifications/tickets`, `/classification-attempts`, `AUTO_ON_CREATE`, `MANUAL_REANALYSIS`, `classificationAttemptId`, `AI_CLASSIFICATION_APPLIED`, `classifierVersion`, `confidence`, `Closed tickets cannot be classified`, and `failed attempts do not change ticket category or priority`.
    - `docs/sdd/phase-04-ai-classification-integration-api.md` contains `Phase 4 Exclusions`.
    - `docs/sdd/phase-04-ai-classification-integration-api.md` contains `no provider-backed LLM`, `no prompts`, `no RAG`, `no embeddings`, `no vector search`, `no draft generation`, `no async jobs`, `no frontend UI`, `no authentication`, and `no full RBAC`.
    - `README.md` contains `ai-service-python`, `python -m pytest`, `docker compose`, and `SUPPORTFLOW_AI_CLASSIFICATION_BASE_URL`.
    - `cd backend-spring && ./mvnw test -Dtest=OpenApiDocumentationTest,FoundationVerificationTest` exits 0.
  </acceptance_criteria>
  <done>Phase 4 APIs, local run commands, and exclusions are documented and visible through automated documentation checks.</done>
</task>

<task id="04-03-03" type="auto">
  <name>Task 3: Run and preserve full Phase 4 verification</name>
  <files>ai-service-python/tests/test_classifier_rules.py, ai-service-python/tests/test_classification_api.py, backend-spring/src/test/java/com/supportflow/ticket/TicketClassificationMongoIntegrationTest.java, docs/sdd/phase-04-ai-classification-integration-api.md</files>
  <read_first>.planning/phases/04-ai-classification-integration/04-VALIDATION.md, .planning/phases/04-ai-classification-integration/04-AI-SPEC.md, ai-service-python/pyproject.toml, backend-spring/pom.xml, docker-compose.yml, docs/sdd/phase-04-ai-classification-integration-api.md</read_first>
  <action>Run the full Phase 4 verification sequence: `cd ai-service-python && python -m pytest`, then `cd ../backend-spring && ./mvnw verify`. If a failure appears, fix the implementation or test fixture without changing Phase 4 scope. Confirm Python tests cover deterministic contract behavior and Spring verify covers backend unit, WebMvc/OpenAPI, Mongo/Testcontainers, and Docker-backed FastAPI integration tests. Ensure no test is skipped because Docker is unavailable when Docker is running; existing `@Testcontainers(disabledWithoutDocker = true)` behavior may remain for environments without Docker, but local Phase 4 validation should record Docker-backed tests as runnable. Do not update production code to weaken validation, remove tenant isolation checks, or bypass AI-service integration.</action>
  <verify>
    <automated>cd ai-service-python && python -m pytest && cd ../backend-spring && ./mvnw verify</automated>
  </verify>
  <acceptance_criteria>
    - `cd ai-service-python && python -m pytest` exits 0.
    - `cd backend-spring && ./mvnw verify` exits 0.
    - `backend-spring/src/test/java/com/supportflow/ticket/TicketClassificationMongoIntegrationTest.java` remains present after full verification.
    - `docs/sdd/phase-04-ai-classification-integration-api.md` remains present after full verification.
    - No Phase 4 implementation file contains `OpenAI`, `Anthropic`, `LangChain`, `LangGraph`, `embedding`, `RAG`, `draft response`, `@Async`, or `Redis` as required runtime behavior.
  </acceptance_criteria>
  <done>Phase 4 has green Python and Spring verification gates, including Docker-backed cross-service coverage.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend-spring && ./mvnw test -Dtest=TicketClassificationMongoIntegrationTest` exits 0 with Docker running.
- [ ] `cd backend-spring && ./mvnw test -Dtest=OpenApiDocumentationTest,FoundationVerificationTest` exits 0.
- [ ] `cd ai-service-python && python -m pytest && cd ../backend-spring && ./mvnw verify` exits 0.
- [ ] `docker-compose.yml` wires `ai-service` and backend classification base URL.
- [ ] `docs/sdd/phase-04-ai-classification-integration-api.md` documents contracts, artifacts, history traceability, failure behavior, tenant isolation, and exclusions.
</verification>

<success_criteria>
- Backend-to-FastAPI classification works through a reproducible local Docker boundary.
- Classification artifacts are visible to operational consumers through ticket APIs.
- Successful classification updates are traceable through workflow history linked to attempts.
- Failed classification attempts remain visible and non-mutating.
- Full Python and Spring verification passes for Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-classification-integration/04-03-SUMMARY.md`.
</output>
