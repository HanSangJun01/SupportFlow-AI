---
phase: 04-ai-classification-integration
plan: 04-01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-service-python/pyproject.toml
  - ai-service-python/Dockerfile
  - ai-service-python/app/__init__.py
  - ai-service-python/app/main.py
  - ai-service-python/app/models.py
  - ai-service-python/app/classifier.py
  - ai-service-python/app/settings.py
  - ai-service-python/tests/test_classifier_rules.py
  - ai-service-python/tests/test_classification_api.py
autonomous: true
requirements: [AI-01, AI-02, QUAL-04]
requirements_addressed: [AI-01, AI-02, QUAL-04]
user_setup: []
---

<objective>
Scaffold the FastAPI AI service with a deterministic, contract-tested ticket classification endpoint.

User story: As a support operations platform maintainer, I want a local FastAPI classification service that returns stable category, urgency, sentiment, priority, confidence, and classifier version outputs for ticket content, so that the Spring backend can integrate with a reliable AI-service contract before retrieval or draft generation exists.

Purpose: Deliver the first callable Phase 4 vertical slice: a Python service with strict Pydantic request/response models, deterministic keyword rules, health and classification routes, Docker runtime, and pytest contract coverage.
Output: `ai-service-python` FastAPI application, Pydantic models, deterministic classifier, tests, and Dockerfile.
</objective>

<execution_context>
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hansangjun/Desktop/SupportFlow-AI/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ai-classification-integration/04-CONTEXT.md
@.planning/phases/04-ai-classification-integration/04-AI-SPEC.md
@.planning/phases/04-ai-classification-integration/04-RESEARCH.md
@.planning/phases/04-ai-classification-integration/04-VALIDATION.md
@.planning/phases/04-ai-classification-integration/04-PATTERNS.md
</context>

<must_haves>
- AI-SPEC: selected framework is FastAPI + Pydantic v2 direct service with deterministic in-process rules; do not introduce LLM providers or agent frameworks.
- D-01/D-02/D-03: Response must include `category`, `urgency`, `sentiment`, `priority`, `confidence`, and `classifierVersion`; urgency/sentiment/priority use fixed enum strings; confidence is numeric `0.0..1.0`.
- D-04/D-26: Do not add human-readable rationale, prompts, provider calls, network-backed inference, or nondeterministic AI behavior.
- D-23/D-24/D-25: Use transparent keyword rules over subject/message with rule-derived confidence bands.
- D-05: Every successful response includes `classifierVersion`.
- AI service remains stateless and does not persist tenants, tickets, attempts, or users.
- QUAL-04: Python tests must cover classification contract and deterministic rules.
</must_haves>

<threat_model>
| Threat ID | STRIDE | Component | Disposition | Mitigation Plan |
|-----------|--------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | AI service contract | mitigate | Use Pydantic models with `extra="forbid"` and bounded `confidence`; contract tests assert required fields and enum values. |
| T-04-01-02 | Repudiation | Classifier versioning | mitigate | Include `classifierVersion = "rules-v1"` in every successful response and tests. |
| T-04-01-03 | Denial of Service | Oversized classification input | mitigate | Add subject/message max lengths in Pydantic models and tests for validation failures. |
| T-04-01-04 | Scope Creep | AI framework/provider usage | mitigate | Do not add OpenAI, LangChain, LangGraph, embeddings, prompts, RAG, async queues, or provider secrets. |
</threat_model>

<tasks>

<task id="04-01-01" type="auto">
  <name>Task 1: Scaffold FastAPI package, models, and health/classification routes</name>
  <files>ai-service-python/pyproject.toml, ai-service-python/Dockerfile, ai-service-python/app/__init__.py, ai-service-python/app/main.py, ai-service-python/app/models.py, ai-service-python/app/settings.py, ai-service-python/tests/test_classification_api.py</files>
  <read_first>.planning/phases/04-ai-classification-integration/04-AI-SPEC.md, .planning/phases/04-ai-classification-integration/04-RESEARCH.md, .planning/phases/04-ai-classification-integration/04-PATTERNS.md, ai-service-python/.gitkeep, docker-compose.yml</read_first>
  <action>Create a real Python service under `ai-service-python`. Add `pyproject.toml` with project name `supportflow-ai-service`, Python requirement `>=3.12`, runtime dependencies `fastapi[standard]==0.136.3`, `pydantic==2.13.4`, and test dependencies `pytest==9.0.3`, `httpx>=0.27,<1`. Add pytest config so `python -m pytest` discovers `tests`. Add `Dockerfile` based on `python:3.12-slim`, set `WORKDIR /app`, copy `pyproject.toml`, install the package with test/runtime deps as needed for runtime, copy `app`, expose `8000`, and run `fastapi run app/main.py --host 0.0.0.0 --port 8000` or equivalent uvicorn command. Add `app/models.py` with `StrEnum` classes `Urgency` values `LOW`, `NORMAL`, `HIGH`, `CRITICAL`; `Sentiment` values `NEGATIVE`, `NEUTRAL`, `POSITIVE`; `Priority` values `LOW`, `MEDIUM`, `HIGH`, `URGENT`. Add `ClassificationRequest` with `tenantId`, `ticketId`, `subject`, `customerMessage`, `ConfigDict(extra="forbid", str_strip_whitespace=True)`, subject length `1..200`, and message length `1..5000`. Add `ClassificationResponse` with `category`, `urgency`, `sentiment`, `priority`, `confidence = Field(ge=0.0, le=1.0)`, and `classifierVersion`. Add `app/main.py` with `app = FastAPI(title="SupportFlow AI Service", version="0.1.0")`, `GET /health` returning `{"status":"ok"}`, and `POST /api/v1/classifications/tickets` using `response_model=ClassificationResponse`. Add `app/classifier.py` with a deterministic `classify_with_rules` function that returns a schema-valid response for valid requests; Task 2 will expand the keyword table and confidence bands. Add API tests using `fastapi.testclient.TestClient` that assert `/health` returns 200 and `POST /api/v1/classifications/tickets` returns all required response fields for a valid request. Remove or leave unused `.gitkeep` without depending on it.</action>
  <verify>
    <automated>cd ai-service-python && python -m pytest</automated>
  </verify>
  <acceptance_criteria>
    - `ai-service-python/pyproject.toml` contains `fastapi[standard]==0.136.3`, `pydantic==2.13.4`, `pytest==9.0.3`, and `httpx`.
    - `ai-service-python/Dockerfile` contains `python:3.12-slim`, `EXPOSE 8000`, and either `fastapi run` or `uvicorn`.
    - `ai-service-python/app/models.py` contains `class ClassificationRequest`, `class ClassificationResponse`, `extra="forbid"`, `confidence`, `ge=0.0`, and `le=1.0`.
    - `ai-service-python/app/main.py` contains `FastAPI(title="SupportFlow AI Service"`, `@app.get("/health")`, and `@app.post("/api/v1/classifications/tickets"`.
    - `ai-service-python/tests/test_classification_api.py` contains `/health`, `/api/v1/classifications/tickets`, `classifierVersion`, and `confidence`.
    - `ai-service-python` contains no string `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `LangChain`, `LangGraph`, `embedding`, or `prompt`.
    - `cd ai-service-python && python -m pytest` exits 0.
  </acceptance_criteria>
  <done>The Python AI service is installable, containerizable, and exposes strict health/classification routes.</done>
</task>

<task id="04-01-02" type="auto">
  <name>Task 2: Implement deterministic keyword classifier and reference dataset tests</name>
  <files>ai-service-python/app/classifier.py, ai-service-python/app/models.py, ai-service-python/tests/test_classifier_rules.py, ai-service-python/tests/test_classification_api.py</files>
  <read_first>.planning/phases/04-ai-classification-integration/04-CONTEXT.md, .planning/phases/04-ai-classification-integration/04-AI-SPEC.md, .planning/phases/04-ai-classification-integration/04-RESEARCH.md, ai-service-python/app/models.py, ai-service-python/app/main.py, ai-service-python/tests/test_classification_api.py</read_first>
  <action>Implement `app/classifier.py` as a pure deterministic rule component. Define `CLASSIFIER_VERSION = "rules-v1"`. Define `classify_with_rules(request: ClassificationRequest) -> ClassificationResponse`. Normalize text by joining subject and customerMessage, lowercasing, and collapsing whitespace. Implement ordered keyword groups: billing/payment category for keywords `bill`, `billing`, `payment`, `invoice`, `refund`, `charge`, `card`; technical category for `bug`, `error`, `broken`, `crash`, `cannot load`, `outage`, `down`; account category for `login`, `password`, `account`, `locked`, `sign in`, `access`; cancellation category for `cancel`, `cancellation`, `terminate`, `churn`; fallback category `general`. Set urgency/priority to `CRITICAL`/`URGENT` for `outage`, `down`, `security`, `breach`; `HIGH`/`HIGH` for `urgent`, `blocked`, `cannot`, `failed payment`, `cancel`; otherwise `NORMAL`/`MEDIUM` for matched categories and `LOW`/`LOW` for fallback. Set sentiment `NEGATIVE` for `angry`, `frustrated`, `upset`, `terrible`, `unacceptable`; `POSITIVE` for `thanks`, `thank you`, `great`, `appreciate`; otherwise `NEUTRAL`. Use confidence bands: `0.92` for critical/strong multi-signal matches, `0.84` for direct category match with escalation, `0.74` for direct category match, `0.58` for only sentiment/urgency signal, and `0.41` for fallback. Clamp confidence into `0.0..1.0`. Add parametrized pytest cases for billing, technical outage, account access, cancellation, positive billing thanks, neutral general inquiry, fallback low-signal text, and repeated deterministic calls. Add API tests proving extra request fields return 422, blank subject returns 422, blank message returns 422, oversized subject returns 422, oversized message returns 422, and response contains no `rationale` field.</action>
  <verify>
    <automated>cd ai-service-python && python -m pytest</automated>
  </verify>
  <acceptance_criteria>
    - `ai-service-python/app/classifier.py` contains `CLASSIFIER_VERSION = "rules-v1"`.
    - `ai-service-python/app/classifier.py` contains `billing`, `technical`, `account`, `cancellation`, and `general`.
    - `ai-service-python/app/classifier.py` contains confidence values `0.92`, `0.84`, `0.74`, `0.58`, and `0.41`.
    - `ai-service-python/tests/test_classifier_rules.py` contains `pytest.mark.parametrize`.
    - `ai-service-python/tests/test_classifier_rules.py` contains expected categories `billing`, `technical`, `account`, `cancellation`, and `general`.
    - `ai-service-python/tests/test_classifier_rules.py` contains a test asserting repeated calls return equal `model_dump()` values.
    - `ai-service-python/tests/test_classification_api.py` contains assertions for HTTP 422 on extra fields, blank subject, blank message, oversized subject, and oversized message.
    - `ai-service-python/tests/test_classification_api.py` asserts `rationale` is not in the response JSON.
    - `cd ai-service-python && python -m pytest` exits 0.
  </acceptance_criteria>
  <done>The AI service returns deterministic, versioned, rule-derived classifications with contract and reference dataset coverage.</done>
</task>

<task id="04-01-03" type="auto">
  <name>Task 3: Harden FastAPI service contract, OpenAPI visibility, and Docker runtime</name>
  <files>ai-service-python/app/main.py, ai-service-python/app/settings.py, ai-service-python/tests/test_classification_api.py, ai-service-python/tests/test_classifier_rules.py, ai-service-python/Dockerfile, ai-service-python/pyproject.toml</files>
  <read_first>.planning/phases/04-ai-classification-integration/04-AI-SPEC.md, .planning/phases/04-ai-classification-integration/04-VALIDATION.md, ai-service-python/app/main.py, ai-service-python/app/models.py, ai-service-python/app/classifier.py, ai-service-python/Dockerfile, ai-service-python/pyproject.toml</read_first>
  <action>Add `app/settings.py` with constants `SERVICE_NAME = "SupportFlow AI Service"` and `SERVICE_VERSION = "0.1.0"`; use them in `main.py`. Ensure `/openapi.json` includes schema names `ClassificationRequest` and `ClassificationResponse`. Add tests that call `/openapi.json` and assert it contains `/api/v1/classifications/tickets`, `ClassificationRequest`, `ClassificationResponse`, `Urgency`, `Sentiment`, and `Priority`. Add tests proving the service does not require environment variables named `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `PHOENIX_API_KEY`, or `PHOENIX_COLLECTOR_ENDPOINT` to classify a ticket. Ensure `Dockerfile` installs only runtime dependencies for container execution and that `python -m pytest` remains the local verification command. Do not add Phoenix tracing in this plan unless it is optional and disabled by default; do not add any provider SDK.</action>
  <verify>
    <automated>cd ai-service-python && python -m pytest</automated>
  </verify>
  <acceptance_criteria>
    - `ai-service-python/app/settings.py` contains `SERVICE_NAME = "SupportFlow AI Service"` and `SERVICE_VERSION = "0.1.0"`.
    - `ai-service-python/app/main.py` imports `SERVICE_NAME` and `SERVICE_VERSION`.
    - `ai-service-python/tests/test_classification_api.py` contains `/openapi.json`, `ClassificationRequest`, `ClassificationResponse`, `Urgency`, `Sentiment`, and `Priority`.
    - `ai-service-python/tests/test_classification_api.py` contains `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `PHOENIX_API_KEY`, and `PHOENIX_COLLECTOR_ENDPOINT` in a test that verifies they are not required.
    - `ai-service-python/Dockerfile` contains `EXPOSE 8000`.
    - Runtime files under `ai-service-python/app`, `ai-service-python/pyproject.toml`, and `ai-service-python/Dockerfile` contain no string `langchain`, `langgraph`, `llamaindex`, `crewai`, `openai`, `anthropic`, or `embedding`.
    - `cd ai-service-python && python -m pytest` exits 0.
  </acceptance_criteria>
  <done>The FastAPI service contract is documented through OpenAPI, testable locally, and ready for backend integration.</done>
</task>

</tasks>

<verification>
- [ ] `cd ai-service-python && python -m pytest` exits 0.
- [ ] `ai-service-python/app/main.py` exposes `GET /health` and `POST /api/v1/classifications/tickets`.
- [ ] `ai-service-python/app/models.py` defines strict request/response models with bounded confidence.
- [ ] `ai-service-python/app/classifier.py` defines deterministic keyword rules and `CLASSIFIER_VERSION = "rules-v1"`.
- [ ] No provider-backed LLM, prompt, embedding, RAG, queue, Redis, frontend, auth, or draft behavior is introduced.
</verification>

<success_criteria>
- The AI service returns category, urgency, sentiment, priority, confidence, and classifier version in a testable format.
- The same input produces the same output across repeated classifier and API calls.
- Invalid inputs fail through FastAPI/Pydantic validation.
- Python tests prove the classification contract and local deterministic behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-classification-integration/04-01-SUMMARY.md`.
</output>
